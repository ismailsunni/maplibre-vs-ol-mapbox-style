{
  "name": "maplibre-vs-ol-mapbox-style",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.6.0",
  "description": "Render the same MapLibre GL style through MapLibre GL JS and through OpenLayers + ol-mapbox-style, side by side, to compare where the two renderers diverge (e.g. label rendering).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^5.6.0",
    "ol": "^10.9.0",
    "ol-mapbox-style": "^13.4.1"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
